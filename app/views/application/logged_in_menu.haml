%ul.nav.pull-right
  %li.divider-horizontal
  %li.dropdown
    %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#" }
      -if current_user.picture.present?
        = image_tag current_user.picture.url(:thumb)
      -else current_user.avatar_img_url.present?
        #{image_tag current_user.avatar_img_url, :style => "width: 40px; height: 40px;"}
      = current_user.name
      %strong.caret
    %ul.dropdown-menu{:style => "padding: 15px;"}
      %li
        = link_to "Account",  edit_user_registration_path()
      %li.divider-horizontal
      %li
        = link_to "Manage NGO's", manage_ngo_path()

      %li
        = link_to "Sign Out", destroy_user_session_path(), :method => :delete

